<%= form_for(@architect) do |f| %>
  <% if @architect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@architect.errors.count, "error") %> prohibited this architect from being saved:</h2>

      <ul>
      <% @architect.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
